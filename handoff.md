# 配网终端通讯 · 进度交接（handoff）

更新日期：2026-09-11  
仓库：https://github.com/IveNoIdeaWhatNameICanUse/peiwang-terminal-comm.git  
本地路径：`D:\project\规约工具及文档\配网终端通讯`  
分支：`main`（与 `origin/main` 同步；最新功能提交见下文）

---

## 1. 产品目标（已确认）

- Windows 桌面工具：IEC 60870-5-104 / **101 模拟主站**
- 支持四遥测试、绑定本地固定 IP（含指定本地端口、多网卡选择、连接失败分类提示）
- 产品形态：桌面；接受 GPL；101 优先平衡式，并支持非平衡 / **海南双主站**
- 协议层：自研 Python（满足本地 bind）；后续可桥接 c104 / lib60870（可选）

---

## 2. 当前进度摘要

### 已完成

| 项 | 状态 | 说明 |
|---|---|---|
| 开源调研与技术方案 | 完成 | 见 `2026-09-07-配网终端通讯开源调研与技术方案.html` |
| 104 MVP + 多主站 | 完成 | Tk 默认；固定本地端口重连（SO_LINGER）；多 session |
| PyInstaller / 离线依赖 | 就绪 | `scripts/build_exe.py`、`scripts/install_offline_wheels.py` |
| IEC 101 平衡/非平衡 | 完成 | FT1.2 + 主站；KW-2200 时序对齐；`scripts/sim_iec101.py` |
| 101 可变帧校验和 | 已修 | CS **仅对用户数据**（控制+地址+ASDU）求和；L=18 对时不再错 CS |
| 忽略 FCB 位错误 | 完成 | 101 参数勾选；勾选后用户数据 FCV=0/FCB=0 |
| 设备参数（周期任务） | 完成 | 独立「设备参数」按钮；总召/校时/心跳（心跳仅 101） |
| 定值 202 回读写四遥 | 已修 | 101 `MONITOR_TYPES` 含 55/108/202/203，与 104 对齐 |
| 固化/撤销清「修改值」 | 完成 | 成功后清空遥调修改值列（固化成功可先提交再清） |
| 海南双主站模式 | 完成 | **平衡链路** + AA 封装 + 同 COM 按中心编号分流 |
| 去掉 101 自动落盘桌面 | 完成 | 不再在断开时写桌面报文文件；手动导出仍可用 |

### 未做 / 后续

- **UI 重写（PySide6 + QSS，规避加密软件拦 HTML/WebView）**：Qt 界面已实施并成为默认入口（`PEIWANG_USE_TK=1` 回退 Tk），待打包实测与现场 DLP 验收，**详见 `handoff-ui.md`**
- 101 / 海南双主站更多实机联调（加密模块多隧道、中心号现场约定）
- 非平衡「一级数据主动上送 + ACD 轮询」实机时序调优
- 更完整点表/工程管理体验
- 与 c104 / lib60870 桥接（可选）
- 清理全仓库 `AGENT_CHANGE` 标记（用户未确认清理前保留）
- README 部分反引号损坏，需整理
- 本地工程 JSON（`configs/default.json`、`104*.json`）一般不入库

---

## 3. Git / 工作区状态（交接时）

**远端 / 本地 `main`：** 已同步（`origin/main...HEAD` 无超前无落后）。

**近期关键提交（节选）：**

- `7e2cc2b` feat(101): 海南双主站、设备周期参数、校验和与定值体验修复
- `705decc` 及更早：101 现场时序 / KW-2200 对齐、握手诊断等

**工作区可能仍有（勿误当核心代码提交）：**

- 已修改：`configs/default.json`（本地默认工程/点表，体积大）
- 未跟踪：`104双主站.json`、`104四主站.json`、`104海南双主站.json` 等工程导出

> 打包产物 `dist/`、`build/`、截图一般不入库。

---

## 4. 架构速览

```
app/main.py              # 入口：默认 Tk；可选 WebView
app/tk_shell.py          # Tk UI（多主站、101/104/设备参数、四遥、报文）
ui/index.html + app.js   # WebView 备用（与 Tk 功能大致同步）
core/api.py              # ApiBridge：会话/连接/四遥/定值/工程 API
core/project.py          # SessionDef（含 link_mode、center_id、设备周期字段）
protocol/iec104/         # 104 编解码 + Master（含周期总召/对时）
protocol/iec101/
  link.py                # FT1.2；可变帧 CS=sum(用户数据)
  master.py              # 101 主站（平衡/非平衡/海南）
  hainan.py              # 海南 AA 封装 + 同 COM Hub 分流
net/                     # 网卡枚举
configs/                 # 默认工程（本地常改，提交需谨慎）
scripts/build_exe.py     # 打包
scripts/sim_iec101.py    # 101 自测
```

### 多主站要点

- `sessions`：每路独立参数；104 为远端/本地 IP·端口；101 为串口/链路
- 总召/对时/遥控/定值走**当前激活会话**
- 海南双主站：多会话可共用同一 COM，靠 `center_id` 分流

---

## 5. 如何运行

### 源码

```bash
cd D:\project\规约工具及文档\配网终端通讯
python -m app.main
```

可选 WebView：`set PEIWANG_USE_WEBVIEW=1` 后同上（本机加密环境常拦 HTML，默认勿开）。

### 打包

```bash
python scripts/build_exe.py
```

产物：`dist\配网终端通讯\配网终端通讯.exe`（整目录发布）。旧 exe 占用 `dist` 会导致 WinError 5。

### 自测 101

```bash
python scripts/sim_iec101.py
python scripts/sim_iec101_api.py
```

---

## 6. 建议验证清单

1. **104 重连**：固定本地端口 → 连 → 断 → 立即再连（无 10048）
2. **多主站 104/101**：两路同时在线，切换会话做总召/遥控/定值
3. **101 对时**：可变帧 CS 正确；规约分析工具可解析（勿再出现 L=18 错校验）
4. **设备参数**：设短周期总召/校时/心跳 → 重连后报文监视可见周期帧；104 不显示心跳项
5. **定值**：参数全召唤后遥调「值」更新；固化成功或撤销成功后「修改值」清空
6. **海南双主站**：两会话同 COM、中心号 1/2；TX 为 `AA … 中心号 … 55`；无周期召 2 级；控制域按平衡（如 `C0`/`F3`/`D2`）

---

## 7. 环境与已知坑

| 项 | 说明 |
|---|---|
| Python | 本机约 3.10.10；PyInstaller 5.9.0 |
| 加密软件 | 拦截本地 HTML/WebView；默认 Tk |
| `AGENT_CHANGE` | 源码中大量标记；清理需用户明确同意 |
| 校验和 | 标准=只对用户数据求和；`cs_compat` 再 `^0x80`（默认关）；连接后可探测 |
| 忽略 FCB | 兼容从站 FCB 异常；勾选后控制域如 `C3`（无 FCV），规范终端请勿勾 |
| 海南模式 | **平衡 101 + AA**；勿再理解成非平衡召 2 级 |

---

## 8. 下一任接手建议顺序

1. 按第 6 节验证 101 定值 / 设备参数 / 海南双主站
2. 实机联调海南加密模块多通道（需求文档：`关于海南加密通信模块多通道设计需求.docx`）
3. 确认后清理 `AGENT_CHANGE`；整理 README
4. 需要时再 push 之外的本地工程 JSON（勿把大点表误提交为默认配置）

---

## 9. 101 使用要点

### 链路模式（`link_mode`）

| 模式 | UI | 行为 |
|---|---|---|
| 非平衡 | 非平衡 | 周期召 2 级 / ACD 召 1 级 |
| 平衡 | 平衡 | DIR/FCB；双方可发起；心跳 FC=2 为 `D2`/`F2` |
| 海南双主站 | 海南双主站 | **平衡链路** + `protocol/iec101/hainan.py` AA 封装；`center_id` 1~255；同 COM Hub 分流 |

- 会话字段：`link_mode`、`center_id`（兼容旧字段 `balanced`）；新建会话自动分配未占用中心号（常见 1、2）
- AA 帧：`AA \| L(2B LE=N) \| 中心编号 \| 业务N字节 \| CS \| 55`；CS=从 L 到数据末累加和
- 示例（文档）：业务 `10 49 01 4A 16` → `AA 05 00 01 10 49 01 4A 16 C0 55`

### 其它 101 参数

- 串口：`serial_port`、`baudrate`、`serial_parity`（**默认 N 无校验**）、`stopbits`、`link_addr`、`addr_size`（默认 2；**海南双主站默认 1**）、`ioa_size_101`（默认 2）、`poll_period`、`tx_delay_ms`、`ignore_fcb_error`
- DIR/校验和：连接后自动探测（UI 已去掉说明长文，逻辑仍在）
- **设备参数**（101/104 共用按钮）：`clock_period`（分，默认 10）、`gi_period_min`（分，默认 15）、`heartbeat_period`（秒，默认 30，**仅 101 显示**）；0=禁用；保存后重连生效
- 可变帧 CS：只对用户数据求和（对时 L=18 与总召 L=12 均正确）

### 现场（KW-2200 / F30 类）备忘

- 平衡、2 字节链路地址、常见 9600、IOA 2 字节
- 复位 `C0` → 确认；从站主动 `49` → 主站 `8B`；链路建立后自动总召
- 国网定值：202 召唤 / 203 预置·固化·撤销；回读须写入遥调点表

---

## 10. 关键对话结论备忘

- 默认 UI：**Tk**；不要恢复内嵌 UI，除非明确要求
- 不默认 git commit / push；用户说「提交」再提交；本次功能已进 `7e2cc2b`
- 海南双主站：**平衡 + AA**，不是非平衡召 2 级
- 组召唤周期：已明确不做；设备参数仅总召 / 校时 / 心跳
- 本地 `configs/default.json` 与 `104*.json`：工程数据，提交时默认排除
